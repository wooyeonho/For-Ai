$ulw-loop "PR 3: raw markdown Supabase fallback.

Product guardrails:
- Core document content must be readable from raw HTML/Markdown without client-side JavaScript.
- GYEOL is claim-level: entities -> documents -> claims -> claim_sources -> verification_events.
- documents.data is rendering convenience only.
- No fake facts; unknown facts must show '확인 필요' and confidence low.

Allowed file scope:
- app/raw/[...path]/route.ts
- lib/render.ts
- lib/supabase-documents.ts

Task:
Apply Supabase fallback to the raw markdown route when static seed lookup misses.

Acceptance criteria:
- Raw Markdown output remains claim-level and source-aware.
- Unknown or missing facts are explicitly marked '확인 필요' with low confidence.
- Route paths stay consistent with canonical wiki/API URLs.
- Run npm run lint, npm run build, and npm run ci:guards.

PR description must include:
- What is more discoverable: Supabase-backed documents become available through raw Markdown endpoints.
- AI citation safety: raw Markdown exposes claims with source/confidence context and avoids unsourced invented facts."
