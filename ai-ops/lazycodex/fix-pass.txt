$ulw-loop "Run a focused fix pass for GYEOL using the latest discovery-pass results.

Product guardrails:
- GYEOL is not an AI wiki.
- GYEOL is a local fact registry for AI, search engines, and humans.
- Static-first rendering is mandatory.
- Core document content must be readable from raw HTML without client-side JavaScript.
- schema-v3.sql is the source of truth; do not use schema-v2.sql or invent a different database model.
- entity_id is mandatory from MVP.
- English slug is stable; display titles are language-specific.
- GYEOL is claim-level.
- Canonical structure: entities -> documents -> claims -> claim_sources -> verification_events.
- documents.data is rendering convenience only and must not become the canonical source of factual truth.
- No fake facts. Unknown facts must show '확인 필요' and confidence low.
- Do not invent venue details.
- llms.txt is secondary, not the legal basis, and not the citation engine.
- Public read is not allowed for edits, reports, or hallucination_reports.
- Public submissions do not require login.
- Never store raw IP addresses; store contributor_hash only.
- Do not add payment, user accounts, complex auth, or large admin systems in the MVP.

MVP target:
- entity_id: kr-weddinghall-laluce-001
- slug: myungdong-laluce-parking
- page: /ko/wiki/myungdong-laluce-parking

Fix scope:
1. Read the discovery-pass results.
2. Choose exactly one P0 issue.
3. Make the smallest safe patch for that one P0 only.
4. Do not fix multiple P0 issues in a single pass.
5. Split independent concerns into separate passes. For example, route coverage should be handled as one isolated unit rather than bundled with unrelated static rendering, schema, privacy, or mojibake fixes.
6. Do not invent facts or venue details.
7. Do not change schema-v3.sql unless the selected P0 explicitly requires it and the change preserves the canonical claim-level model.
8. Avoid broad rewrites, new product surfaces, account systems, payment flows, complex auth, or large admin systems.

After the patch, run these checks in order:
- npm run lint
- npm run build
- npm run ci:guards

If any check fails, either fix only failures directly caused by this patch or revert the patch and report why. End with a summary containing the selected P0, changed files, exact commands run, results, risks, and remaining P0s not touched in this pass."
